select interpretation, count(1) from (select unnest as interpretation from normalized_snv s join clinvar c on s.chromosome=c.chromosome and s.start=c.start and s.reference=c.reference and s.alternate=c.alternate, unnest(c.interpretations) as unnest where s.sample_id=%s) t group by interpretation order by 2 desc;