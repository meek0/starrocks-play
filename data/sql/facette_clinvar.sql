select c.interpretation, count(1) from occurrences o left join clinvar_exploded c on o.chromosome=c.chromosome and o.start=c.start and o.reference=c.reference and o.alternate=c.alternate where o.sample_id=%s and c.interpretation is not null group by c.interpretation order by 2 desc;